# Session Log: Phase 6 Spike — CLI Discovery & Attach Semantics

**Session:** Phase 6 Spike (Days 1–2)  
**Date:** 2026-05-09  
**Agent:** Carter (Bridge Dev, claude-sonnet-4.6)  
**Scope:** Answer Q1 (CLI discovery) and Q2 (attach semantics) from locked Phase 6 design  

---

## Outcome Summary

**Q1 SOLVED** ✅ — Discovery via SDK `client.listSessions()` works today. HIGH confidence.

**Q2 BLOCKED** ⚠️ — True bidirectional attach requires `--ui-server` + port discovery. No port breadcrumb file exists. Aaron must decide: drop `/attach` from MVP, or ship config-based workaround.

---

## What Was Tested

1. **SDK API (`listSessions()`)** — Real public API, returns SessionMetadata[]. Confirmed via `.d.ts` types and `client.js` impl.
2. **Shared session store** — `~/.copilot/session-state/` exists with UUID subdirs + `workspace.yaml`. Confirmed on disk.
3. **Live session detection** — `inuse.{PID}.lock` files present. Signal interpreted as active session (not confirmed by docs, inferred from name + lock semantics).
4. **`--ui-server` mode** — Documented in SDK README + `.d.ts`. Not confirmed in Aaron's installed CLI version (risk: HIGH).
5. **Port breadcrumb** — Searched `~/.copilot/`. No port file found anywhere. Conclusion: port discovery gap is real.

---

## Three Paths Forward (Evaluated)

| Path | Effort | Semantics | Risk |
|------|--------|-----------|------|
| **A: Config-based port** | Low | Shared output | Requires Aaron coordination on CLI flags + config |
| **B: Breadcrumb wrapper** | Medium | Shared output | Needs adoption of wrapper script |
| **C: PID → port lookup** | Low | Shared output | Windows-only; fragile; requires lock file presence |

**Recommended for MVP:** Option 1 (no `/attach`). Option 2 as Phase 6 stretch.

---

## Risk Mitigations

| Risk | Severity | Mitigation |
|------|----------|-----------|
| No `--ui-server` in Aaron's CLI | HIGH | Test early: `gh copilot --help \| grep ui-server` |
| Lock file reliability | MEDIUM | Document that live detection requires infinite-sessions enabled |
| Two-process conflict | HIGH | Guard in `cliDiscovery.ts`: warn/block if attaching to live session in stdio mode |
| Port mismatch (Path A) | MEDIUM | Config validation at startup; document requirement clearly |

---

## Concrete Next Steps

**If Option 1 (MVP no `/attach`):**
1. Implement `src/discovery/cliDiscovery.ts` — read `listSessions()` + cross-ref lock files
2. Implement `src/control/session0.ts` — route `/list`, `/new`, `/afk`, `/back` (no `/attach`)
3. No config changes needed

**If Option 2 (MVP + `/attach` Path A):**
1. All of Option 1, plus:
2. Add `REACH_CLI_SERVER_URL` to config.json schema
3. In `src/relay/relay.ts`: swap SDK construction to use `cliUrl` when available
4. Document Aaron's launch requirement: `--ui-server --port <PORT>`

---

## Summary

Spike complete. Q1 has a clear path (SDK API). Q2 requires scope decision from Aaron. Implementation can begin once scope is locked.
