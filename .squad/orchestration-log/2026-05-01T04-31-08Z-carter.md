# Orchestration Log — Carter

**Date:** 2026-05-01T04:31:08Z  
**Phase:** Phase 4, Wave 2  
**Mode:** background  
**Outcome:** success

## Work Completed

- Updated `src/relay/relay.ts` with optional `bot` and `permissionPolicy` constructor parameters
- Implemented per-topic permission callback creation for `interactiveDestructive` mode
- Integrated permission callbacks into session creation flow
- Maintained backward compatibility with existing relay behavior

## Artifacts Modified

- `src/relay/relay.ts` — Added optional params:
  - `bot?: Bot` — Telegram bot instance for permission prompting
  - `permissionPolicy?: string` — Permission policy mode (defaults to 'none')

## Implementation Details

- Creates per-topic permission callback when `permissionPolicy === 'interactiveDestructive'`
- Callback routes permission requests to bot via Kat's prompt handler
- Passes callback to `CopilotSessionFactory.create()` and `.resume()`
- Session creation fails with clear error if permission mode requires callback but none available

## Test Status

- `npm run lint` ✅
- `npx tsc --noEmit` ✅
- `npx vitest run` — 162 passed, 4 skipped

## Key Decisions

1. **Optional parameters** — Relay remains functional without bot/permission policy; adds features when injected
2. **Per-topic callbacks** — Each topic gets its own permission callback, enabling independent approval workflows
3. **Fail-fast on misconfiguration** — If interactiveDestructive mode requested but no bot provided, error immediately

## Cross-Cutting Impact

- Relay is now the orchestration point for permission callbacks
- Bot instances can be injected at relay startup
- Permission policy can be configured per-deployment

## Next Steps

- Jun: Integration tests covering permission prompt flow
- Team: E2E testing with real Telegram bot
