# Orchestration Log: jun — ADR-9 Test Scenario Catalog

**Date:** 2026-05-22T20:30:05Z  
**Agent:** jun (Test Engineer, sonnet-4.6, background)  
**Task:** Phase 6 Day 5 — Draft 29-scenario test catalog for ADR-9

---

## Deliverable

**File:** `.squad/decisions/inbox/jun-adr9-permission-test-scenarios.md` (194 lines)

- **Test scenario catalog:** 29 test scenarios across 6 categories:
  - **Category 1 (Happy path):** 6 scenarios — approve, deny, allow-always/deny-always policies, prompt ordering (before/mid-stream)
  - **Category 2 (Timeout/cancellation):** 5 scenarios — user never replies, session ends, pipe drop mid-prompt, iterator abandoned, pipe drop before response delivery
  - **Category 3 (Correlation/ordering):** 5 scenarios — concurrent prompts same session, out-of-order responses, interleaved with other sessions, multiple sessions each with prompts, post-stream prompt
  - **Category 4 (Adversarial/edge):** 8 scenarios — foreign requestId, malformed request, response after timeout, response after session abandoned, duplicate responses, listener cleanup, unregistered socket, oversized frame
  - **Category 5 (Regression hooks):** 5 scenarios — relay 800ms throttle unaffected, MarkdownV2 escaping intact, splitter behavior unchanged, non-stalling concurrent sessions, session.event passthrough
  - **Category 6 (Protocol ambiguities):** 8 open questions for Noble Six

- **Ambiguity table:** 8 protocol questions locked (P1–P8) requiring Noble Six's ADR-9 to resolve before test implementation begins. Each question maps to specific scenario impact.

- **Test doubles contract:** Specifies `FakeDaemon`, `FakeExtensionClient`, `FakeBridge` additions needed:
  - `FakeDaemon`: Permission request/response message types, auto-respond modes, pending query
  - `FakeExtensionClient`: Send/receive permission messages, auto-respond for inject→prompt→response cycles
  - `FakeBridge`: Emit permission events (conditional on P1 resolution)

- **Summary table:** 29 scenarios by category; key risk areas flagged.

---

## Status

**DRAFT** — all 29 scenarios written, 8 ambiguities flagged for Noble Six's ADR-9, test doubles contract specified. Ready for implementation once ADR-9 resolves P1–P8.

---

## Integration Notes

- **Noble Six's ADR-9 answers 8 protocol questions** that directly affect test structure (message type names, requestId ownership, timeout ownership, reconnect behavior, etc.). Jun's 29 scenarios cannot be finalized until these are locked.
- **Test double contract is forward-compatible:** Additions are conditional on protocol decisions (e.g., FakeBridge permission event depends on whether ADR-9 uses top-level `permission.request` or `session.event` with discriminator).
- **No test implementation has started.** Jun awaited ADR-9 wireup (planned coordination). Now both deliverables are ready; Jun can begin test-double extensions and scenario skeleton-writing pending Aaron's ADR-9 decisions.

---

## Source Gaps Noted

- `BridgeSession` currently silently drops `permissionCallback` (accepted gap in Days 3–4 decision).
- `PermissionPrompter` port exists but unwired on bridge path (ADR-9 will wire it).
- Jun's test design reflects current state accurately and will track implementation faithfully.
