# Orchestration Log: noble six — ADR-9 Draft

**Date:** 2026-05-22T20:30:05Z  
**Agent:** noble six (Lead/Architect, sonnet-4.6, background)  
**Task:** Phase 6 Day 5 — Draft ADR-9 (Permission Prompting Over Bridge)

---

## Deliverable

**File:** `.squad/decisions/inbox/noble-six-adr9-permission-prompting.md` (345 lines)

- **Problem statement:** `BridgeSession` accepts but silently discards `permissionCallback`. Destructive tools (run_bash, write_file) execute without user consent over the bridge.
- **Decision:** In-stream interleaving on existing pipe (ADR-3). New message types: `permission.request` (ext→daemon), `permission.response` (daemon→ext), `permission.cancelled` (ext→daemon).
- **Wire schema:** Full JSON shapes with field descriptions, risk level guidance, timeout semantics, correlation by `permissionId`.
- **Adapter integration:** `ExtensionBridge` gains 3 new message types + `sendPermissionResponse()` helper. `BridgeSession` registers permission listener + manages allow-always cache. `PermissionPrompter` port unchanged (boolean-returning stub).
- **Security:** Risk level enforcement, `allow-always` scoping options, audit logging, message validation.
- **Alternatives:** Out-of-band pipe (rejected; ADR-3 violation), pre-approval policy file (rejected for interactive use).
- **Trade-offs:** 8 documented (in-stream concurrency, single-pipe blocking, per-session cache, daemon timeout, etc.). All accepted.
- **Open questions for Aaron:** 4 questions locked — Telegram UX shape (§9.1), default timeoutMs (§9.2), allow-always scope (§9.3), risk-classification ownership (§9.4).
- **Implementation work:** 6 tasks for Kat (§10).
- **Test scenarios:** 10 scenarios for Jun (§11).
- **ADR index:** All 9 ADRs now present; ADR-9 status = PROPOSED.

---

## Status

**PROPOSED** — awaiting Aaron's decisions on 4 open questions before implementation begins.

**Gating:** Production dogfooding is blocked. ADR-9 must be resolved before Kat starts permission-prompting work.

---

## Integration Notes

- **Jun's test plan** is ready (`jun-adr9-permission-test-scenarios.md`, 29 scenarios + 8-item ambiguity table).
- **Carter's bridge protocol** (ADR-8) is already finalized; ADR-9 extends it transparently.
- **Kat's relay refactor** (Days 3–4) is unblocked; this ADR is a future gate before her permission-prompting feature work begins.

---

## History Updated

Added note to `Kat history.md` and `Carter history.md`: ADR-9 drafted, 4 open questions flagged, implementation gates Kat's permission-prompting work.
