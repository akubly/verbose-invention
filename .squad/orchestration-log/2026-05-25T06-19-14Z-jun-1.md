# Jun-1 Orchestration Log

**Session:** Phase 7 implementation kickoff  
**Spawn Time:** 2026-05-25T06:19:14Z  
**Agent:** jun-1 (claude-sonnet-4.6)  
**Model:** sonnet  
**Mode:** background  

## Mandate

8 test cases (T1–T8) from round-1 audit covering /afk → topic → /back lifecycle, extended FakeDaemon/FakeExtensionClient with ADR-11 §4 message helpers, integrated T4 negative-test (Telegram /back ignored) and T6 no-retry clarifications.

## Outcome

✅ **Typecheck/lint clean; 30/31 integration green at write-time**

- Added `tests/integration/afk-mode.contract.test.ts` covering T1–T8 from round-1 audit.
- Extended `tests/helpers/FakeDaemon.ts` with ADR-11 message types:
  - `afk.request`, `back.request`, `afk.activated`, `back.confirmed`, `mode.changed`, `mirror.input`, `relay.command`
  - Helpers: `expectAfkRequest()`, `expectBackRequest()`, `sendAfkActivated()`, `sendModeChanged()`, `sendMirrorInput()`
- Extended `tests/helpers/FakeExtensionClient.ts`:
  - `sendAfkRequest()`, `sendBackRequest()`, `sendSessionEvent()`
  - `expectAfkActivated()`, `expectModeChanged()`, `expectMirrorInput()`
- Added `tests/helpers/afkContract.ts` for Telegram API spies, relay-target spy, SessionEntry fixtures with/without `lastTopicId`, `mode`, varied `cwd`.

**Protocol clarifications captured:**
- **T4:** Rewritten as negative test per ADR-11 §2. Telegram `/back` must NOT emit `back.confirmed`, must NOT change mode, must NOT close topic.
- **T6:** Updated per ADR-11 §10. `/afk` with daemon unreachable errors immediately with `⚠ Reach daemon not running — start it first.` and creates no retry/backoff timers.

**Test status:** 30/31 green (T4 correctly red-flagging missing Telegram `/back` filter since potentially addressed by Kat).

## Decision Log

See `.squad/decisions/inbox/jun-phase7-test-cases.md` (merged into decisions.md 2026-05-25T06:19:14Z).

## Status

COMPLETE — Convergence signal T4 red is expected; filter should land in Kat's daemon integration.
