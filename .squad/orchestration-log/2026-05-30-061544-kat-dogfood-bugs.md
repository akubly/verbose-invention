# Orchestration Log: Kat Dogfood Bugs #3 & #4 Fix

**Timestamp:** 2026-05-30T06:15:44Z  
**Agent:** Kat (claude-sonnet-4.6)  
**Session Role:** Bot Dev  
**Branch:** `user/aaron/dogfood-bugs-3-4`  
**Commit:** `f78ccd6` (pushed, awaiting PR)

## Summary

Fixed two dogfood-prep bugs in extension:

**Bug #3: Duplicate "Back at desk" Banner**
- Cause: `handleModeChanged(active=false)` and `handleBackConfirmed` both emitted banner
- Fix: Made `mode.changed(false)` silent; `back.confirmed` owns the banner
- Invariant: Only one source emits for any transition
- Tests: 5 new unit tests covering handler contract

**Bug #4: Session ID Duplicate in Topic Title**
- Cause: `SESSION_NAME` fallback to `SESSION_ID` (UUID), making title show "sess-abc (sess-abc)"
- Fix: Changed fallback to `path.basename(process.cwd())` for meaningful project name
- Robustness: `SESSION_ID` retained as final fallback per ADR-8 §3
- Future-proof: Automatic if CLI later sets `SESSION_NAME` env var

**Files Changed:**
- `extension.mjs` (banner + SESSION_NAME)
- `tests/bridge/extension-back-banner.test.ts` (new)

**Validation:**
- 538 tests passed / 4 skipped / 0 failed
- tsc clean, lint zero warnings

**Status:** Branch ready; holding for Phase 8.5 merge (Aaron's instruction).
