# Carter — Phase 8.5 Task 2 Execution Log

**Timestamp:** 2026-05-30T00:00:02Z  
**Agent:** Carter (Bridge Dev)  
**Model:** claude-sonnet-4.6  
**Mode:** background (multi-turn)  
**Task:** Phase 8.5 Task 2 — Full orchestrator, uninstaller, dev junction

---

## Summary

✅ **COMPLETE** — Full install orchestration pipeline implemented. 570 tests passed / 4 skipped / 0 failed.

## Deliverables

- `src/install/index.ts` — 180 lines. Config wizard (TTY-gated, env-var-aware prompts), copyExtension sync call, service install async call, next-step printer before process.exit.
- `src/install/uninstall.ts` — 90 lines. Extension dir removal, optional LOCALAPPDATA wipe (--wipe flag), service uninstall call.
- `src/install/copyExtension.ts` enhanced — Added dev mode (NODE_ENV=development) junction creation. Lines 49–75 implement `mklink /J` via cmd.exe.
- `npm run init` script added to package.json — Runs `node dist/install/index.js` (config wizard + extension + service install, full flow).
- `npm run uninstall` script added to package.json — Runs `node dist/install/uninstall.js` (supports --wipe flag).

## Decision Records

- **FS removal pattern:** Use `fs.existsSync` + `fs.rmSync({ recursive: true, force: true })` instead of `lstatSync` + conditional unlinkSync/rmSync. Rationale: Matches Jun's mock expectations; rmSync handles junctions correctly on Windows.
- **Source file check location:** Moved `existsSync(sourcePath)` guard inside production else branch. Dev mode skips check because junction is already accessible.
- **Orchestrator flow:** Config → copyExtension → print → install (async, handles own exit). Service install exits via node-windows event handler, so no code after it runs.
- **Uninstaller ordering:** removeExtension → wipeLocalData (if --wipe) → uninstall (service). Service uninstall exits in handler, so all cleanup must happen first.

## Test Status

- `npx vitest run` — **570 passed / 4 skipped / 0 failed** ✅
- New tests: 33 (copyExtension extended TC10–TC14 junction mode, uninstall UN1–UN6, index IX1–IX13)
- Existing tests: 537 passing
- `npx tsc --noEmit` ✅ clean
- `npm run lint` ✅ clean

## Verification Notes

- Config wizard: TTY-gated; non-TTY + missing vars = exit 1 with instructions
- Junction mode (dev): Creates symlink when NODE_ENV=development; skips source file check
- Uninstall --wipe: Deletes LOCALAPPDATA\GitHub Copilot\reach if present
- Service integration: Async install/uninstall via node-windows patterns (event handlers call process.exit)
- Readline mocking: Via vi.hoisted() answer queue for multi-prompt flows

## Acceptance Criteria Met

✅ `npm run build && npm run init` runs full flow (config → extension → service)  
✅ `npm run uninstall` removes extension dir + service  
✅ `npm run uninstall -- --wipe` also clears LOCALAPPDATA state  
✅ NODE_ENV=development creates junction (dev shortcut for Aaron)  
✅ All 570 tests green  
✅ Unblocks Phase 8.5 sprint completion

## Blocked/Deferred

- Cross-platform support (Windows-only per Phase 8.5 scope)
