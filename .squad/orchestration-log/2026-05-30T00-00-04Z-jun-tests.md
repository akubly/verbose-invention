# Jun — Phase 8.5 Task 3 + Task 2 Tests Execution Log

**Timestamp:** 2026-05-30T00:00:04Z  
**Agent:** Jun (Test Engineer)  
**Model:** claude-sonnet-4.6  
**Mode:** background (multi-turn)  
**Task:** Phase 8.5 Task 3 + Task 2 tests — Test coverage for copyExtension, uninstall, index orchestrator

---

## Summary

✅ **COMPLETE** — Test infrastructure for Phase 8.5 install story. 33 new tests, all GREEN.

## Deliverables

- `tests/install/copyExtension.test.ts` — 14 tests (TC1–TC14)
  - TC1–TC9: Happy path, mkdir, APPDATA validation, source file check, error handling
  - TC10–TC14: New junction mode tests (NODE_ENV=development)
- `tests/install/uninstall.test.ts` — 6 tests (UN1–UN6) — New file
  - Extension dir removal, LOCALAPPDATA wipe (--wipe flag), service integration
- `tests/install/index.test.ts` — 13 tests (IX1–IX13) — New file
  - Config wizard prompts (multi-prompt readline mock), service install integration, TTY detection

## Decision Records

- **Test file location:** `tests/install/` (not `tests/unit/install/`) — matches established repo convention
- **FS mocking:** `vi.mock('fs', async (importOriginal) => { ... })` with spread actual — same pattern as `tests/service/install.test.ts`
- **process.exit spy:** Throw instead of swallow — enables `expect(() => fn()).toThrow('process.exit(1)')`
- **Readline mock:** Hoisted answer queue (`rlAnswerQueue: string[]`) — clean multi-prompt simulation
- **TTY detection:** `Object.defineProperty(process.stdin, 'isTTY', { configurable: true, value: false })`
- **Mock clarity:** `clearAllMocks()` (not `restoreAllMocks()`), then re-establish implementations
- **Relative mock paths:** `vi.mock('../../src/install/copyExtension.js')` — Vitest resolves both import paths to same file
- **Service mock exports:** Full shape (`install`, `uninstall`, `createService`, `resolveCurrentUser`, `promptPassword`) to prevent "not a function" errors

## Test Count Summary

| File | Tests | Status |
|------|-------|--------|
| tests/install/copyExtension.test.ts | 14 | 14/14 GREEN ✅ |
| tests/install/uninstall.test.ts | 6 | 6/6 GREEN ✅ |
| tests/install/index.test.ts | 13 | 13/13 GREEN ✅ |
| **Total** | **33** | **33/33 GREEN** ✅ |

## Verification Notes

**copyExtension.test.ts (TC1–TC9):**
- Happy path: copy + log
- First install: mkdir with recursive
- Upgrade: idempotent overwrite
- APPDATA unset: exits 1
- No Copilot CLI: exits 1 with detected message
- Source file missing: exits 1 with source message
- Spaces + unicode in APPDATA path
- mkdir EPERM: exits 1 with error
- copyFileSync EPERM: exits 1 with error

**copyExtension.test.ts (TC10–TC14 — Junction Mode):**
- NODE_ENV=development: creates junction instead of copy
- Skips source file check in dev mode
- Correct log message: `/linked|dev/i` pattern

**uninstall.test.ts (UN1–UN6):**
- Remove reach/ dir
- LOCALAPPDATA preservation (default)
- --wipe flag behavior
- Service uninstall integration
- Error handling

**index.test.ts (IX1–IX13):**
- Config wizard prompts for missing env vars
- Multi-prompt readline queue
- TTY detection (interactive vs non-interactive)
- copyExtension called on success
- Service install called on success
- Exit paths (empty input, missing vars in non-TTY)

## Carter Implementation Contract

Tests pass against Carter's implementation. If Carter changes `copyExtension.ts` or `index.ts`, these contracts must hold:

1. **copyExtension():** Exports `export function copyExtension(): void` (sync)
2. **runInit():** Exports `export async function runInit(): Promise<void>` (async)
3. **runUninstall():** Exports `export function runUninstall(opts: UninstallOptions): void`
4. **Interface:** `UninstallOptions { wipe: boolean }`

## Acceptance Criteria Met

✅ 33 new tests, all GREEN  
✅ Full coverage of copyExtension (9 + 5 junction mode = 14)  
✅ Full coverage of uninstall (6 tests)  
✅ Full coverage of orchestrator (13 tests)  
✅ `npx tsc --noEmit` ✅ clean  
✅ `npx vitest run` ✅ all 570 tests pass

## Blocked/Deferred

None. All tests green, infrastructure complete.
