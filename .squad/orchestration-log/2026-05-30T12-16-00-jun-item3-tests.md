# Orchestration Log — Jun Item 3 Tests: CWD Registry (Phase 9)

**Date:** 2026-05-30T12:16:00-07:00  
**Agent:** Jun (Test Engineer)  
**Task:** Phase 9 Item 3 — Tests for config helpers, /cwd commands, /new --cwd flag  
**Status:** ✅ Complete / All Green

## Agent Assignment

- **Role:** Test Engineer
- **Model:** claude-sonnet-4.6
- **Context:** Write tests for Kat's helpers (T5) and Carter's commands (T6+T7)

## Deliverables

### Test Files

| File | Scope | Count | Status |
|---|---|---|---|
| `tests/config/knownCwds.test.ts` | Kat's 8 helpers | 60 tests | ✅ PASS |
| `tests/bot/cwdCommand.test.ts` | Carter's `/cwd` group | 12 tests | ✅ PASS |
| `tests/bot/newCwdFlag.test.ts` | Carter's `/new --cwd` | 11 tests | ✅ PASS |

**Total: 83 tests — All GREEN**

### Test Architecture

**Mock approach:**
- `vi.hoisted()` + `vi.mock()` for config module and helpers
- Matches existing vi.mock pattern from Item 2 tests
- Stable against internal refactors; tests observable surface (ctx.reply, registry.register calls)

**Fake timers:**
- Used `vi.useFakeTimers({ now: new Date('2026-01-15T10:00:00Z') })`
- Freezes `new Date().toISOString()` for deterministic timestamp assertions

### Key Test Decisions

1. **ConfigPath API** — Tests assume `HandlerOptions.configPath?: string` (matched Carter exactly)
2. **Missing configPath** — Tests cover guard: `/cwd` and `/new --cwd` without configPath return error
3. **Active session removal** — Test confirms `/cwd remove` succeeds even if alias in use (sessions hold resolved path, not reference)
4. **Same path under two aliases** — Test confirms allowed (path uniqueness not enforced)
5. **General Topic detection** — Tests use `ctx.message?.message_thread_id === undefined` pattern
6. **Dangling flags** — Tests confirm `/new --model` without value detected and rejected

### Test Coverage

**knownCwds.test.ts (60):**
- All 8 helpers covered
- Alias validation: valid, invalid, reserved
- Path comparison: case-sensitivity on Windows
- Lookup by alias/path
- Add/remove/touch operations
- Sorting: lastUsedAt desc, then alias asc

**cwdCommand.test.ts (12):**
- `/cwd list` success, empty list
- `/cwd add` success, alias collision, invalid alias, invalid path
- `/cwd remove` success, unknown alias
- General Topic only enforcement
- Config not set error

**newCwdFlag.test.ts (11):**
- Flags position-independent: `name --cwd value`, `--cwd value name`
- Path vs alias disambiguation
- Dangling flags detected
- Config not set error for --cwd
- Registry.register called with correct CWD arg

### Known Observations

- **Windows-only path detection** — Noted; Unix branch (`startsWith('/')`) deferred Phase 10
  - Tests use platform-agnostic `ABS_PATH` constant
  - On Linux CI, Unix paths accidentally route through alias lookup (acceptable for Phase 9)

### Coordination Notes

- Carter's code matched test assumptions exactly (no adjustments needed)
- Kat's helpers matched function signature expectations
- No pre-existing test failures introduced

## Outcome

Item 3 test suite comprehensive, green, and provides regression baseline for multi-CWD feature.
